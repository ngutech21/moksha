{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "wallet mint ",
      "type": "shell",
      "command": "cargo", // note: full path to the cargo
      "args": ["run", "--bin", "moksha-wallet", "mint", "10"],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
